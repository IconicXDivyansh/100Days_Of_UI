---
title: Sticky Footer
description: Sticky Footer
layout: "../../../layouts/DemoLayout.astro"
containerType: "contained"  
---
import RandomUnderline from "@/components/common/RandomUnderline.astro";
import DemoContainer from "@/layouts/DemoContainer.astro";
import Demo from "./demo.tsx";

<DemoContainer width="default" padding="false" >
    <Demo client:load />
</DemoContainer>

<div class="max-w-7xl mx-auto">
    <div class="text-[10px] md:text-[12px] text-center italic mt-4 mb-10">
        sticky footer demo
    </div>
</div>
<RandomUnderline />
<div className="md:max-w-4xl mx-auto mt-10 p-1 rounded-xl bg-linear-to-r bg-black/5 via-white  to-black/5 border border-border">
<div className=" p-2 rounded-[9px] bg-white border border-border">
    Properties used: useInView
    Logic : Since, footer is always visible (we're simply playing with z-index , it's actually behind the main content but visible all the time), we can't use intersectionObserver or whileInView etc.
    Solution: We place a element on the end of the main content, as this element enters the viewport, we know that footer will be visually revealed that's when we animate the footer. 
</div>
</div>